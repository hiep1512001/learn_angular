import { CapitalpipePipe } from './capitalpipe.pipe';

describe('CapitalpipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalpipePipe();
    expect(pipe).toBeTruthy();
  });
});
